# מבט מהצד במחברת — Scribble side-view (spec)

> **הצפון:** *אותה תחושה של להיות במחברת* כמו ב-`scribble_dungeon`, אבל מהצד.
> זה — ולא "פלטפורמר" — הוא מה שקובע כל החלטה כאן. הפלטפורמינג הוא רק אמצעי
> התנועה על הדף; הוא לעולם לא שער-כישרון שחוסם ילד שיודע את התשובה.

מסמך זה מתאר איך לבנות מבט-צד בסגנון המחברת המצוירת, בהתבסס על החבילה
**Kenney "Scribble Platformer" (CC0)** — החבילה-האחות של Scribble Dungeons
(100 אסטים, אריחי 64×64 PNG, קו-עיפרון שחור על שקיפות, אותו רישיון). כי הסגנון
זהה, הכול נצבע מחדש בזמן ריצה עם אותו `inked()` הקיים ומתלבש על כל ערכות הנושא.

---

## 0. ההחלטה הפתוחה (במכוון) — איך זה "נארז"

המשתמש עוד לא החליט אם המבט-צד יהיה **קטעי קישור** בתוך המבוך, או **שלבים נפרדים**.
לכן התכנון מפריד בין *ליבה משותפת* (שנבנית פעם אחת ומשרתת את שתי הצורות) לבין
*האריזה* (שכבה דקה שנבחרת בסוף). אין צורך להחליט עכשיו.

| | **מצב C — קטעי קישור** (בתוך `scribble_dungeon`) | **מצב L — שלבים נפרדים** (`scribble_platformer` חדש) |
|---|---|---|
| מה זה | המעבר בין שני חדרים מצויר לפעמים מהצד: הדמות רצה וקופצת לאורך המסדרון, ואז חוזרים למבט-על | סבב מלא מהצד: מסלול נגלל עם כמה פלטפורמות-תשובה; קופצים אל הנכונה, והמסלול נמשך ומצויר קדימה |
| appType חדש? | לא — וריאנט של אנימציית המעבר ב-scribble-dungeon | כן — `scribble_platformer`, route משלו, כניסות תפריט |
| שאלה משלו? | לא בהכרח — השאלה כבר קרתה ליד הדלת; הקטע יכול להיות מעבר טהור (ראו §6, החלטה 2) | כן — כל סבב הוא שאלה עם פלטפורמות-תשובה |
| היקף | קטן ואלגנטי; "רקמת החיבור" של המבוך הופכת למבט-צד | משחק-אח מלא; מופרד נקי |
| סיכון | ערבוב שני מודלי מצלמה/קואורדינטות במשחק אחד | יותר קוד, אבל מבודד |

**המלצה:** לבנות קודם את **הליבה המשותפת** (§3), ולהריץ אותה ראשית כ**שלב עצמאי
(מצב L)** — קל יותר לפתח בבידוד ולראות אם התנועה מרגישה טוב. רק אז להחליט אם
גם לחווט אותה כקטעי-קישור (מצב C) בתוך המבוך. הקוד זהה; מצב C הוא אינטגרציה
מאוחרת ואופציונלית מעל אותה ליבה.

---

## 1. מה ממחזרים מ-`scribble_dungeon` (הרוב כבר כתוב)

`games/scribble-dungeon.js` כבר מכיל כמעט את כל התשתית של "מחברת מצוירת חיה".
נעביר מ שם, מילה-במילה או כמעט:

| רכיב קיים | תפקיד | שינוי נדרש |
|---|---|---|
| `inked(name, color, paper)` + `inkCache` | צביעה דו-גונית של כל ספרייט לפי הנושא (קו-עיפרון→`ink`, מילוי לבן→`paper`) | אין — verbatim |
| `THEME_INK` + `resolveScribbleTheme` | נייר+עיפרון ל-6 הנושאים (base/soldiers/unicorn/space/dark/code) | אין |
| רקע המחברת | נייר, שריג משובץ, קו-שוליים אדום | אין — אותו ציור רקע |
| `HAND_FONT` / `SIGN_FONT` / ציור שלטים | הטקסט בכתב-יד על הדף | אין |
| `SIGN_SLOTS` / `slotsFor` | מיקומים קבועים לתשובות (הילד לומד לאן להסתכל) | להתאים לפריסה אופקית לאורך המסלול (§4) |
| `mulberry` + `roomSeed`/salt | RNG דטרמיניסטי → אותו קטע מצויר אותו דבר בכל frame ואחרי resize | אין — אותה גישה, "segmentSeed" |
| חוזה הלמידה: `generateFromList` → `answer` → `onCorrect`/`onWrong` → `updateWeightForKey` → `reloadProgress` → `saveScore` | כל מנגנון השלבים/המשקלים/ה-news | אין — מועתק כמעט as-is |
| `hotspots`: `<button>` אמיתיים מעל הקנבס | נגישות, מקלדת, קורא-מסך (הקנבס נושא את האמנות, הכפתורים את הסמנטיקה) | אין |
| `fx`/`motes` + `puff`/`shake` | חלקיקים, רעד, פידבק | אין |
| `playSound`, HUD ניקוד/gems, `pace` (רגיל/מהיר/איטי), מסך-מלא, החלפת דמות | כל שכבת ה-HUD | אין |
| לולאת RAF + `beforeDestroy` (`roundToken`, `_timers`, `own()`/`dur()`/`rate()`) | ניהול זמן ואנימציה + ניקוי משאבים ביציאה | אין — אותה שלד |
| `cam {x,y,z,tx,ty,tz}` + `aimCamera`/`snapCamera` | מצלמה עם יעד חלק | להוסיף גלילה אופקית (§4) |
| `beginWalk` + `samplePath` | הדמות צועדת אוטומטית לאורך polyline | **מפתח:** `samplePath` הופך לקשת הקפיצה (§4) — ממחזרים אותו לפרבולה |

> המסקנה: מבחינת עבודה, ~70% מהמנוע כבר קיים. מה שחדש הוא רק ה"פיזיקה מהצד"
> (קרקע, פלטפורמות, קפיצה) והגלילה האופקית.

---

## 2. תחושת המחברת — מה חייב להישמר (הצ'קליסט של ה"וייב")

אלה הדברים שאם אחד מהם נשבר, אבדה התחושה שהמשתמש אוהב:

1. **דף משובץ אמיתי** — אותו נייר, שריג ב-pitch 64, קו-שוליים אדום. אותו רקע בדיוק.
2. **הכול בעיפרון** — כל ספרייט עובר `inked()`; שום טקסטורה צבעונית "מודבקת".
3. **מצויר תוך כדי** — קטע חדש נכנס בליטוש reveal (0→1), כמו חדר שנחשף. הקו
   "נמתח" על הדף בזמן שהדמות מתקדמת.
4. **מה שצויר נשאר** — הדף גדל; קטעים קודמים לא נמחקים. מצלמה נגללת, הציור נשאר.
5. **טעות = שרבוט X** על הדף — כמו דלת חסומה; ה"דרך שלא נבחרה" נשארת מצוירת ומסומנת.
6. **כתב-יד** — שאלות ותשובות ב-`HAND_FONT`, לא פונט מסך.
7. **קצב לצפייה** — פקד ה-`pace` הקיים; זה משחק שרואים אותו מצויר, לא משחק תגובות.

---

## 3. הליבה המשותפת — "מנוע מחברת מהצד"

מודול חדש `games/scribble-platformer.js`, באותו תבנית IIFE של scribble-dungeon,
חושף `createScribblePlatformerComponent(BaseGameComponent)` ואת הפונקציות הטהורות
לבדיקות (כמו ש-scribble-dungeon חושף את הגנרטור שלו ל-vm).

### 3.1 מודל העולם (מהצד)
- **קרקע** = קו אחד מהוקק על הדף (baseline), מצויר מאריחי ground/planks של החבילה.
- **כובד** פשוט: הדמות יושבת על הקרקע/פלטפורמה; קפיצה = פרבולה בין נקודת-פתיחה
  ליעד (לא סימולציה מלאה). את הקשת דוגמים עם `samplePath` הקיים.
- **פלטפורמות/מדפים** מצוירים מהצד מאריחי החבילה (בלוקים, קרשים, קצוות).
- **קואורדינטות**: world-x גדל ימינה ללא הגבלה (הדף נמשך); world-y סביב ה-baseline.
- **decor** דטרמיניסטי לפי seed: שיחים, עננים, פטריות, ארגזים, אויב-נוי (מתנדנד
  עם `WOBBLE` הקיים). קישוט בלבד — אף פעם לא חוסם.

### 3.2 סבב (מצב L; מצב C מדלג על חלק "השאלה")
1. `prepareRound()` → `generateFromList(...)` נותן `question`, `result`, `options`
   (זהה לשורות ב-scribble-dungeon).
2. פורסים **N פלטפורמות-תשובה** לאורך הקטע הבא, בגבהים ברי-קפיצה; על כל אחת
   שלט-תשובה אחד (סלוטים קבועים לאורך המסלול — §4). התשובה הנכונה תמיד ביניהן.
3. הילד מקיש על פלטפורמה (ה-`<button>` שלה). הדמות **רצה וקופצת** אליה אוטומטית.
4. **נכון** → נחיתה, דגל/מטבע, `updateWeightForKey(-1)`, ניקוד + בונוס-מהירות,
   הקטע הבא נמתח ימינה בליטוש reveal, המצלמה נגללת. (זהה ל-`onCorrect`.)
5. **טעות** → הפלטפורמה מתפוררת / משורבטת ב-X, הדמות נשארת, `updateWeightForKey(+1)`,
   פידבק. הנכונה נשארת נגישה. (זהה ל-`onWrong`.)
6. סיום שלב/פתיחת מילים חדשות/מסך news — הכול דרך `reloadProgress` של ה-Base.

### 3.3 תנועה — אוטו-ריצה (ברירת מחדל)
כמו ה-pawn שצועד לבד ב-scribble-dungeon: בוחרים תשובה, והדמות רצה+קופצת אליה.
זה שומר על ה-DNA של "משחק שרואים אותו מצויר" ועל הנגישות (אין כישלון-בגלל-מיומנות).
שליטה ידנית (D-pad+קפיצה, כמו ב-`platformer` הקיים) היא toggle אופציונלי מאוחר,
לא ברירת מחדל (§6, החלטה 3).

### 3.4 מסכים והתקדמות ביניהם

שתי רמות: המסכים **החיצוניים** (בירושה מה-Base + router, לא כותבים) והמסכים
**הפנימיים** בתוך `/play` (מה שמעצבים כאן).

**חיצוני (קבוע, 4 מסכים):**
`menu` → `/app` (כניסה: "שחק" + "הצג הכל" + רשת מילים עם מנעולים והיסטוריה) →
`/display/news` (המילים החדשות שנפתחו) → `/play`. בסיום שלב `reloadProgress`
מחזיר ל-`/app`.

**היחידה היא חדר, בדיוק כמו במבוך — לא "עמוד".** ההתקדמות היא חדר→חדר, והמפה
המצוירת גדלה ב-2D (ימינה, מעלה במגדל, ומסתעפת במסילה) — כמו שתוכנית המבוך גדלה
ב-scribble-dungeon.
- **מסלול (run)** — כל הריצה, בלתי מוגבל.
- **חדר (room)** — מסך אחד עם *מכניקה* משלו (דלתות / ירי / קפיצה / מגן / גשר /
  מגדל / ארגזים / מסילה — §3.5). כל חדר = שאלה אחת, ותשובותיה מפעילות את המכניקה.
- **מעבר** — כשעונים נכון החדר נפתח והדמות נעה לחדר הבא, שנרשם על הדף תוך כדי.
- *(היפוך-עמוד נשאר קישוט אופציונלי: כשהתוכנית חוצה את שולי הדף, מתהפך דף חדש.)*

> במכונת-המצבים למטה, `hopping` הוא במובן הרחב "המכניקה של החדר פועלת"
> (קפיצה/ירי/טיפוס/דחיפה...), ו-`advancing` הוא מעבר לחדר הבא — לא בהכרח עמוד.

**מכונת-המצבים הפנימית (מראה את `g.state` של scribble-dungeon):**

| מצב | מה רואים |
|---|---|
| `drawing` | "מצייר את הדף…", העמוד הראשון נכנס ב-reveal (מסך פתיחה) |
| `question` | **המסך המרכזי:** הקטע מצויר, N פלטפורמות-תשובה + שאלה למעלה, input פתוח |
| `hopping` | אחרי טאפ — הדמות רצה+קופצת לפלטפורמה (אוטו); המצלמה עוקבת |
| `resolve` | נכון: נחיתה + דגל/מטבע + הקטע נמתח ימינה. טעות: X + התפוררות + נשארים |
| `advancing` | גלילת מצלמה לקטע הבא (reveal), **או** היפוך-עמוד אם העמוד מלא |
| `overview` | טוגל 🗺 — זום-אאוט לכל מה שצויר על העמוד/המסלול; חזרה בטאפ |
| `finish` | בסיום שלב — דגל-סיום קצר, ואז `reloadProgress` מנווט החוצה ל-`/app` |

**מעברים:**
```
חיצוני:  menu → /app → /display/news → /play → (סיום שלב) → /app ↺

פנימי (/play):
  drawing ─▶ question ─טאפ─▶ hopping ─נחיתה─▶ resolve
                ▲                                   │
     overview ⇄ │            נכון → הקטע נמתח ימינה │
                │                                   ▼
                │              advancing ◀──────────┘
                │                  │
                │      עמוד לא מלא → גלילה ימינה ─────▶ question (הבא)
                │      עמוד מלא    → היפוך-עמוד  ─────▶ question (עמוד חדש)
                │
            סיום שלב (כל המשקלים 0) → finish → חזרה ל-/app
```

**שני מקצבים (זה לב ההתקדמות):**
- **היפוך-עמוד** — תכוף ומרחבי (כל ~3–5 שאלות). "ממלאים דף ועוברים לבא".
- **שלב/מנה** — מדי פעם, אבן-דרך *למידה* (מילים חדשות). מעבר חיצוני
  (`/app`→news→חזרה); אפשר לפתוח בו עמוד חדש כ"פרק".

ברירות-מחדל (לא חוסמות, ניתנות לכוונון): ~4 קטעים לעמוד; היפוך-עמוד מרחבי-טהור
(לא תלוי גבול-מנה) — גבול המנה נשאר המעבר החיצוני ל-`/app`.

### 3.5 מפרט החדרים (מה כל תשובה משפיעה)

בדיוק כמו במבוך: deck של סוגי-חדרים שנחלקים בקצב לכל ריצה. **"דלתות" הוא החדר
הרגיל** (plain — כל תשובה יציאה בפני עצמה); **שאר החדרים הם challenge** — יציאה
אחת משותפת, והתשובות הן המנגנון שפותח אותה.

**עיקרון מאחד:** לכל חדר יציאה אחת. ב-challenge התשובה הנכונה מפעילה את המנגנון
ופותחת את הדרך; טעות נדחית ומסומנת X, אבל **היציאה לעולם לא ננעלת בגלל טעות**
(כמו `onWrong` של challenge ב-scribble-dungeon). התשובה הנכונה תמיד בין האפשרויות.

| # | חדר | סוג | התשובה מחוברת ל־ | תשובה נכונה | טעות | כיוון |
|---|---|---|---|---|---|---|
| 1 | דלתות | plain | כל דלת | נפתחת, רצים+קופצים דרכה לחדר הבא | נטרקת + X (השאר פתוחות) | ימינה |
| 2 | ירי במטרה | challenge | כל מטרה על הקיר | חץ פוגע, המטרה מתנפצת, השער נפתח | החץ מחטיא, המטרה קופצת חזרה | ימינה |
| 3 | קפיצה על הבלוק | challenge | כל בלוק מרחף | הבלוק מחזיק, הופך למדרגה קדימה | הבלוק מתפורר, נופלים חזרה | ימינה |
| 4 | בחירת מגן | challenge | כל מגן במדף | חוסם את מטר החיצים, עוברים | המגן מתנפץ, נדחה | ימינה |
| 5 | בניית גשר | challenge | כל מנוף/קרש | הגשר נמתח מעל התהום (reveal), חוצים | הקרש נופל לתהום | ימינה |
| 6 | טיפוס במגדל | challenge | כל סולם/אחיזה | מטפסים לקומה הבאה (מצלמה מעלה) | הסולם נשבר | מעלה |
| 8 | ארגז אל מתג | challenge | כל ארגז | נדחף עד שיושב על המתג, השער נפתח | לא זז / נופל לבור | ימינה |
| 9 | בחירת מסלול | challenge | כל מסילה מסתעפת | מתג-הפסים נזרק, הקרונית מתגלגלת בה | מובילה לבור + X, המתג מתאפס | מסתעף |

*(#7 הושאר פנוי במכוון.)*

**מקורות ספרייטים — שילוב שתי חבילות ה-CC0 באותו סגנון:**
- כבר ב-**Scribble Dungeons** (על הדיסק): `door_closed/open` (1), `weapon_bow`+`arrow_head`+`trapdoor_round` (2), `shield_curved` (4), `bridge`/`bridge_end`+`trapdoor_square` (5), `crate`/`crate_small`+`trapdoor_square` (8), `track`/`track_curve`/`track_crossing`/`cart` (9).
- מ-**Scribble Platformer** (להורדה): בלוקים/פלטפורמות (3), סולמות + פריימי-טיפוס (6), קרקע, דגל/מטבע, ופריימי דמות הליכה/קפיצה מהצד.

**כיווני התקדמות מגוונים** (כמו שתוכנית המבוך גדלה ב-2D): רוב החדרים ימינה, המגדל
מעלה, המסילה מסתעפת — כך המפה המצוירת גדלה לכל הכיוונים על הדף.

---

## 4. מה חדש מול scribble-dungeon (הדלתא לכתיבה)

1. **`samplePath` → קשת קפיצה.** הפונקציה כבר דוגמת polyline לפי אורך-קשת; מזינים
   לה 3 נקודות (פתיחה → פסגה → יעד) ומקבלים פרבולה. אפשר גם ריצה שטוחה + hop.
2. **גלילה אופקית.** ל-`cam` כבר יש `x/tx`; מוסיפים חוק "המצלמה עוקבת אחרי הדמות
   על ציר X, וקטעים שמאלה נשארים מצוירים". `MIN/MAX_ZOOM` נשארים.
3. **פורש-קטעים (segment generator).** במקום packer של חדרים בגריד — גנרטור
   פשוט הרבה יותר: משמאל-לימין, כל קטע = מקטע קרקע + N פלטפורמות בגבהים ברי-קפיצה.
   דטרמיניסטי לפי `segmentSeed(index)`. (בדיקות: קפיצוּת מובטחת, דטרמיניזם, גיוון.)
4. **פריסת שלטים אופקית.** אנלוג ל-`SIGN_SLOTS`: מיקומים קבועים לאורך המסך (למשל
   3 פלטפורמות: קרוב-נמוך, אמצע, רחוק-גבוה) כדי שהילד ילמד לאן להסתכל.
5. **פריימים של דמות.** ב-scribble-dungeon הדמות ספרייט בודד; לחבילת הפלטפורמר
   ככל הנראה יש פריימי הליכה/קפיצה — **לאשר בהורדה**. נפילה-רכה: אם אין פריימים,
   ספרייט בודד עם הטיה/באונס.
6. **מתפורר במקום נחסם.** ה-X של תשובה שגויה מצויר על פלטפורמה שמתפוררת מעט
   (וריאנט של `onWrong` הקיים).

---

## 5. אסטים (Kenney Scribble Platformer, CC0)

**עדיין לא בריפו** — יש רק `assets/scribble-dungeons/`. שלב ראשון בכל מסלול:

1. להוריד את החבילה ל-`assets/scribble-platformer/` (100 PNG, 64×64).
2. `assets/scribble-platformer/LICENSE-KENNEY.txt` + רישום ב-`ASSET_LICENSES.md`
   (כמו החבילות הקיימות).
3. למפות את הקבצים ל-`SPRITES` list, ולאמת מול הדיסק בבדיקה (מראה את
   `scribble_dungeon_test.js`: "every art file the game asks for exists on disk").

**מיפוי צפוי לפי קטגוריה** (השמות המדויקים יאושרו על הקבצים בפועל):
- קרקע/פלטפורמות: ground/dirt/grass blocks, קרשים, קצוות ופינות
- דמות: idle / walk (כמה פריימים) / jump — **לאשר**
- מטרות ופרסים: flag, coin/gem, sign, star, key, door
- מכשולי-נוי: spikes, saw, מעיין (spring), פטריה, ארגז, שיח, ענן
- אויבי-נוי: slime / fly / snail וכו' (מתנדנדים, קישוט בלבד)

> אם קטגוריה חסרה בחבילה — נופלים חזרה על ספרייט מקביל מ-Scribble Dungeons
> (אותו סגנון בדיוק), בדיוק כמו שקורה היום בין אריחי הדאנג'ן.

---

## 6. החלטות פתוחות (למשתמש)

1. **אריזה: קטעי-קישור (C) / שלבים נפרדים (L) / שניהם.**
   → המלצה: לבנות ליבה משותפת ולהריץ קודם כ-L; להחליט על C אחרי פרוטוטייפ.
2. **אם C: האם לקטע-הקישור יש שאלה משלו, או שהוא מעבר טהור?**
   → מעבר טהור = הכי פשוט ושומר על התחושה (סתם המסדרון מצויר מהצד). שאלה בקטע =
   יותר משחק, יותר מורכבות.
3. **תנועה: אוטו-ריצה (מומלץ) מול שליטה ידנית.**
   → אוטו-ריצה שומרת נגישות + DNA של scribble-dungeon. ידני = toggle מאוחר.
4. **מיקום בתפריט (למצב L):** סלוט חדש בקבוצת ה-shared-progress `ch51`
   (append בלבד — האינדקס הוא **הבא בתור בזמן ההוספה**, כדי שאותה רשימת מילים
   מתקדמת יחד עם מבוך-הקלף ושאר המשחקים), מול כניסת-תפריט עצמאית.
   → המלצה: סלוט ב-`ch51`.
   הערה: המסמך ציין בעבר `grp-ch51-5`, אבל הסלוט הזה נתפס מאז (משחק האזנה).
   אין לקבע מספר מראש — קוראים את `SHARED_GROUPS.ch51.games.length` ביום ההוספה.

---

## 7. קבצים שנוגעים בהם (מראה 1:1 את scribble-dungeon)

**חדשים:**
- `games/scribble-platformer.js` — הרכיב + הפונקציות הטהורות
- `games/scribble-platformer.css` — מראה ה-CSS (כמו `scribble-dungeon.css`)
- `tests/scribble_platformer_test.js` — בדיקות (מראה `scribble_dungeon_test.js`)
- `assets/scribble-platformer/*.png` + `LICENSE-KENNEY.txt`

**נגיעות:**
- `index.html` — `<link>` ל-CSS ו-`<script>` ל-JS, אחרי scribble-dungeon
  (שורות ~27 ו-~1371)
- `tester.js` — יצירת הרכיב (guard כמו שורה 9881) + route (כמו 9922):
  `{path:'/play/scribble_platformer/:currentAppId', ...}` (מצב L).
  למצב C — hook לתוך אנימציית המעבר של scribble-dungeon במקום route.
- `service-worker.js` — הוספת שני הקבצים ל-precache + רשימת הספרייטים
  (שורות 19-20 ו-55-73) + **העלאת גרסת ה-cache**.
- `apps.js` / `groups.js` — כניסת משחק (מצב L), **append בלבד**; עדיף סלוט נוסף
  בסוף קבוצת `ch51` ב-`groups.js` (האינדקס = אורך המערך ברגע ההוספה).
- `ASSET_LICENSES.md`, `GAME_ARCHITECTURE.md`, `HOW_TO_ADD_GAMES.md` — תיעוד
  ה-appType החדש (שורת טבלה).

---

## 8. בדיקות (מראה `scribble_dungeon_test.js`, ריצה ב-vm ללא דפדפן)

- ולידציית config + כל ספרייט נטען מראש; אין כפילויות ב-`SPRITES`.
- כל קובץ אמנות קיים על הדיסק (`assets/scribble-platformer/<name>.png`).
- פורש-הקטעים דטרמיניסטי (אותו index → אותו קטע), הקפיצות תמיד ברות-השגה,
  הגבהים מגוונים.
- `samplePath` (קשת הקפיצה) — כבר נבדק ב-scribble-dungeon; נוודא שהשימוש החדש תקין.
- לכל 6 הנושאים יש פלטת דיו; מפתח לא-מוכר נופל ל-base; דיו ≠ נייר בנושאים כהים.
- מפעל-הרכיב מחווט את ה-Base ומנקה אחריו (`beforeDestroy`: עצירת RAF, `_timers`,
  מאזינים).
- מחזור הלמידה: תשובה נכונה מדווחת **בדיוק פעם אחת**, פותחת ומקדמת; תשובה שגויה
  מדווחת פעם אחת, משרבטת X, ומשאירה את הנכונה נגישה; הקשה כפולה לא מדווחת פעמיים.
- רישומים קיימים: route ב-tester, `<script>`/`<link>` ב-index, קבצים ב-worker,
  כניסה ב-apps/groups.

---

## 9. שלבים לביצוע (אחרי אישור)

1. **אסטים** — הורדת החבילה, LICENSE, מיפוי `SPRITES`, בדיקת קיום-על-הדיסק.
2. **ליבה** — `inked`/themes/רקע-מחברת/HUD/RAF/למידה מועברים; רקע מחברת מצויר מהצד.
3. **פיזיקה** — קרקע, קפיצה (`samplePath` פרבולה), גלילה אופקית, reveal של קטע.
4. **פורש-קטעים** + פריסת שלטים אופקית + hotspots.
5. **סבב מלא (מצב L)** — prepareRound/answer/onCorrect/onWrong מהצד.
6. **בדיקות** ירוקות; **ויזואל** ב-headless (skill `visual-verify`) על 2-3 נושאים.
7. **חיווט** — index/tester/worker/apps(groups)/docs.
8. **החלטה** על מצב C (קטעי-קישור) — אינטגרציה אופציונלית מעל אותה ליבה.

---

## 10. סיכום במשפט אחד

בונים "מנוע מחברת מהצד" שממחזר ~70% מ-`scribble_dungeon` (דיו, נושאים, רקע-הדף,
חוזה-הלמידה, מצלמה, hotspots, ניקוי), מוסיפים רק פיזיקת-צד (קרקע/קפיצה/גלילה)
ופורש-קטעים פשוט — ומריצים אותו קודם כשלב עצמאי, כדי להשאיר פתוחה את ההחלטה אם
הוא יהפוך גם לקטעי-קישור בתוך המבוך. התחושה — דף מחברת שמצויר מהצד תוך כדי משחק —
היא הקנה-מידה לכל החלטה.
