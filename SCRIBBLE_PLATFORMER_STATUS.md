# Scribble Platformer — סטטוס משימות

עדכון אחרון: 2026-07-26. קובץ המשחק: `games/scribble-platformer.js` (קבוצה `ch51`,
בסלוט הפנוי הבא — האינדקס נקבע ביום החיבור, לא מראש).

## תמונת מצב כללית
עיצוב-מחדש של החדרים כך שאלמנט-התשובה הוא חלק אינטגרלי מהמעבר בין השלבים, עם מגוון ויזואלי
("הסתובבות בין טירות"). עובדים **מסך-אחרי-מסך**, עם **אישור מהמשתמש לכל מסך/פריסה חדשים** לפני שילוב.

## אימות
- `node tests/scribble_platformer_test.js` → **12/12** (נכון לריצה האחרונה, לפני עריכות המסילה האחרונות שטרם נבדקו).
- סריקת `tests/*.js` → הכול עובר **חוץ** מ-`scribble_dungeon_test.js` (כשל **קיים-מראש** ולא-קשור: `listName:'hebrewAlphabet'` לא תואם regex של אותיות גדולות).
- אימות ויזואלי: harness צילומי-מסך + viewer עצמאי (ראה "תשתית").

---

## מסכים — סטטוס

| מסך | סטטוס | תיאור |
|---|---|---|
| 🚪 שער הטירה (doors) | ✅ **אושר** | שער מקושת בגובה 2 קוביות; נכון → נפתח + מעבר-דף פנימה; שגוי → **נעול** (רעד + מנעול, לא נעלם). 3 וריאציות: חומה חיצונית / אולם פנימי (עמודים+לפידים) / מצודת-אבן. |
| 🪜 מגדל (tower) | ✅ בוצע | סולם שגוי **נשבר ונופל יחד עם הדמות** (גדם + X בבסיס). 3 וריאציות: מצודת-לבנים / מצודת-אבן+דגל / מגדל-צריח. |
| 🟫 קפיצה (jump) | ✅ בוצע | כל הקוביות מעל תהום דוקרני רחב, עמוד-אבן תחת כל אחת. |
| 📦 חצר הארגז (crate) | ✅ בוצע | קופצים על ארגז: נכון → מתנפץ + מטבע (התפרצות ירוקה); שגוי → מתנפץ ריק. 2 וריאציות (אבן/לבנים). |
| 🛡️ מגן חומה (shield) | ✅ בוצע | הדמות עם **קשת יורה חץ**; נכון → פגיעה + הורדת גשר; שגוי → **החטאה**. |
| 🛤️ מסילה (rail) | 🟡 **איטרציה 9 הושלמה ונבדקה — ממתינה לאישור** | ראה למטה. |
| 🌉 גשר האַמָּה (bridge) | 🟡 **מוקאפ ראשון — ממתין לאישור** | האבנים יושבות עכשיו **על סיפון הקשתות** (חלק מהגשר, לא מרחפות מעל); בוחרים אבן שמשלימה את המעבר, אבן שגויה **נופלת לתהום**. `layout 'span'` מוקם מעל ה-`gap`. צריך אישור-כיוון. |
| ➕ מסך חדש: מעבר-ליד-מגדל-יורים | ⬜ ממתין | מגן טוב = ממשיך, מגן רע = בורח חזרה. צריך מוקאפ+אישור. |

### 🛤️ מסילה — פירוט (איטרציה 4, בעבודה)
מה שהמשתמש הגדיר:
- **3 מסילות שונות מ-3 מקומות שונים** בחדר, כולן בזווית ימינה-כלפי-מעלה.
- **ארוכות מאוד, מפותלות, מצטלבות** — ו**יוצאות מגבולות החדר** ומסתובבות **ברחבי כל הדף, רחוק**.
- ה**מצלמה עוקבת** אחרי הקרונית החוצה (בלי להיתקע בגבולות החדר).
- **נכון:** ממשיך **שמאלה** (בכיוון שהמסילה כבר יוצאת בו) ומתנגש ב**אפקט מעבר-הדף** — הדף
  מתחלף מצד שמאל, לכן ההתנגשות בצד שמאל → עף ונוחת בדף חדש של החדר הבא.
- **2 השגויות:** מתפתלות ומסתיימות **בדיוק מעל נקודת הנחיתה**, ואז נפילה ישרה למטה.

היסטוריית דחיות: v1 (מסילות קצרות מתפצלות) ❌ · v2 (S בנתיבים נפרדים) ❌ · v3 (רמפה משותפת/הצטלבות בתוך החדר) ❌ — "עדיין בגבולות החדר".

איטרציה 4 — **הושלמה** (טסטים 12/12 + צילומי-אימות):
- ✅ `RAIL_TPL` הוגדל לפריסה ענקית שחורגת מהחדר (x עד ~15.5 אריחים, גובה עד ~10.5).
- ✅ מעקב-מצלמה: **הוסרו ה-clamps** של גבולות החדר — המצלמה עוקבת אחרי הקרונית לכל מקום.
- ✅ `EXITS.rail.ridePath`: אחרי כל המסילה — נזרקת מעבר לקצה-המסילה, קשת גבוהה מעל כל הדף,
  וצניחה אל **קצה-הדף השמאלי** (`f.x0 - PITCH*0.5`) → ההתנגשות היא שמפעילה את ה-flip. 4.0 שניות.
- ✅ `FAILS.rail.approach`: נזרקת מעבר לקצה-המסילה, **לולאה חוזרת מעל הדף** ואז צניחה בחזרה
  לרצפת החדר הזה (`f.x0 + PITCH*1.4`). 4.6 שניות.
- ✅ `beginPageFlip`: כשאין `doorBlock` (מסילה) — `snapCamera()` לפני צילום-ה-snapshot, כך
  שה-peel מראה את דף-החדר ולא נייר ריק שהקרונית עפה מעליו.
איטרציה 5 — "יותר סיבובים, יותר הצטלבויות, מלהיב" (**הושלמה**, 12/12 + צילומים):
- ✅ `RAIL_TPL` נכתב מחדש: כל מסילה ~21 נקודות — טיפוס ימינה, **לופ מלא** (5 נקודות סביב מעגל),
  יציאה מעבר לקצה הימני, **חזרה סוערת שמאלה דרך שתי המסילות האחרות**, **לופ שני**, ויציאה
  מעבר לקצה השמאלי. סה"כ **6 לופים** ועשרות הצטלבויות. טווח: x מ-‎-4.2 עד ‎17.6 אריחים, גובה עד 13.
- ✅ רגליים-החוצה נמוכות ורגליים-החזרה גבוהות — כך כל מסילה חוצה את שתי האחרות כמה פעמים.
- ✅ הקרונית עם התווית נשארה ב-index 1 (נמוך, מופרד, קריא) — פריסת התשובות לא נפגעה.
- ✅ משכים עודכנו לאורך החדש: רכיבה נכונה 5.0 שניות, לולאת-כישלון 5.6.
- ✅ `_sp_harness.html`: מצב `over` חדש (מצלמת-על לכל הפריסה; הזום עובר ב-slot של `frac`).
איטרציה 6 — קרונית, מובייל, מעבר-דף וחצים (**הושלמה**, 12/12 + צילומים):
- ✅ **הקרונית והדמות** היו נפרדות: הקרונית צוירה 0.63·SPRITE מתחת לדמות, ובכל הטיה המנוף הזה
  העיף אותה הצידה. עכשיו הרוכב **מורם מהקערה** (`translate -0.45·SPRITE`) והקרונית מצוירת
  **אחרונה, ממורכזת על המסילה** — מנוף אפסי, נסגרת על הרגליים, נקראת כיחידה אחת בכל זווית.
- ✅ **בולטוּת המילה במובייל:** `signMetrics()` חדש — הגדלה ככל שהמסך צר (עד ×1.55) אבל **חסומה
  ע"י מספר התשובות** כדי ששלושה שלטים לא ייחפפו; מסננת-נייר כמעט אטומה (0.93), **הילת-נייר**
  סביב האותיות, ומשקל 700. `signBox` הוא המקור-היחיד — תיבה, גופן, טבעת, חץ ואזור-המגע.
- ✅ **מעבר-דף = דף נקי.** חדרי-מעבר-דף נבנים על **אותן קואורדינטות** של החדר הנעזב, לא נכנסים
  ל-`g.rooms` בזמן ההליכה/רכיבה, ו-`beginPageFlip` עושה `g.rooms = [next]` — לא רואים את
  החדר הקודם בדף החדש.
- ✅ **חצים הפוכים:** החץ יוצא **מהתשובה אל האלמנט** (ראש-החץ על הקובייה/הדלת/הקרונית), לא להפך.

איטרציה 7 — תיקוני-משוב אחרונים למסילה+דלת (**הושלמה**, 12/12 + צילומים):
- ✅ **רכיבה נכונה — שמאלה, לא ימינה.** המצלמה מרכזת את הקרונית לאורך כל הרכיבה, אז מה שנקרא
  הוא **כיוון-התנועה**. הקודם החזיר אותה ימינה בסוף ("עפה לכיוון הלא-נכון"). עכשיו ה-fling
  **ממשיך בכיוון-היציאה של המסילה** (שמאלה-ומטה) בלי חזרה ימינה — עפה נקי מהצד השמאלי, שם
  הקילוף של `drawPageTurn` מתחיל להתרומם. `EXITS.rail.ridePath`, 4.6 שניות.
- ✅ **דלת — ה-pin נסגר סוף-סוף.** ה-iter-6 השאיר את הדמות בקואורדינטות-העולם הישנות אך
  הצמיד את המצלמה לחדר-הבא (מימין) → הדמות יצאה מחוץ-למסך-שמאלה. עכשיו `beginPageFlip` מצלם
  את `pin` (מיקום-המסך של הדמות), מניח אותה בכניסת החדר-החדש, ו**מיישר את המצלמה כך שהכניסה
  יושבת על אותה נקודת-מסך** — הדמות באמת נשארת במקום, והדף נקלף סביבה. easeOut ב-`prepareRound`.
- ✅ **נפילת-כישלון מיושבת חזרה.** `snapFail` (מסילה) קורא `aimCamera()` — מצלמת-הרכיבה
  הרחוקה חוזרת אל החדר תוך כדי הנפילה, במקום שהצניחה תתרחש בזום-רחוק.

איטרציה 8 — משוב על 4 ו-5 + שאלה במובייל (**הושלמה**, 12/12 + צילומים):
- ✅ **כישלון — מסתיים בדיוק מעל הנחיתה.** ב-`FAILS.rail.approach` שתי הנקודות האחרונות חולקות
  את אותו x (`landX`), כך שהסוף הוא **ירידה אנכית ישרה** אל מעל נקודת-הנחיתה; `react` (fall)
  נופל משם ישר למטה. (קודם הסתיים באלכסון והנחיתה לא הייתה מתחתיו.)
- ✅ **נכון — נוחת עם סיבוב אחרי ההתנגשות.** פוזה חדשה `tumble` ב-`drawPawn` (סיבוב מלא של 2π
  שנגמר זקוף). `beginPageFlip` (מסילה) לא מציב את הדמות עומדת — הוא **זורק אותה פנימה מעל
  הכניסה ומצניח אותה בקשת מסתובבת** אל הרצפה, במקביל לקילוף-הדף (`landing` walk, 1.7 שניות,
  `finishWalk` מיישב זקוף). ה-flag `landing` מטופל ב-`finishWalk`.
- ✅ **מובייל — השאלה (לא התשובות).** ההבהרה: הכוונה הייתה ל**טקסט השאלה** שהיה קטן ולא-ברור.
  ב-`scribble-platformer.css` (`@media max-width:640`): השאלה יורדת לשורה נפרדת **מתחת
  לשורת ה-HUD** (`padding-top`), מקבלת כמעט את כל הרוחב, וגדֵלה ל-`clamp(26px,7vw,34px)` משקל
  800 על **רצועת-נייר אטומה** — הדבר הבולט ביותר בדף. (שכבת-ההגדלה של התשובות מ-iter-6 נשארה.)
- ⬜ **נותר:** אישור המשתמש על צילומי-האימות (מובייל + כישלון + נחיתה-מסתובבת).

איטרציה 9 — התנגשות-בקצה, חזרה-חלקה, כפתורים, ומוקאפ-גשר (**הושלמה**, 12/12 + צילומים):
- ✅ **נכון — תחושת התנגשות בקצה.** ב-`step`, ברבע האחרון של הרכיבה (`u>0.72`) המצלמה **מפסיקה
  לרדוף** ומתייצבת על חדר-המקור, כך שהקרונית חוצה מסגרת יציבה ו**עפה אל הקצה השמאלי** (שם
  הקילוף מתחיל) — במקום שהקרונית תישאר ממורכזת ואז חיתוך לדפדוף נפרד.
- ✅ **שגוי — חזרה חלקה (מחליף את "תלוי-ונופל").** `FAILS.rail`: הקרונית עולה מעט על המסילה, ואז
  **צוללת מטה ומתעקמת ימינה** וחוזרת בגלישה חלקה בדיוק לנקודת-ההתחלה — בלי נפילה דרמטית.
- ✅ **כפתורי-התשובה מכוונים.** הבאג: ה-hotspots חושבו **פעם אחת** ב-`syncHotspots`, אבל המצלמה
  ממשיכה להתייצב אחריו (בעיקר אחרי דפדוף), אז הבועות (שמצוירות מ-`signBox` כל פריים) זזות
  והכפתורים נשארו במקום הישן. תוקן: `refreshHotspots` **כל פריים** בזמן שאלה, ממקם מחדש לפי
  index. `hotRect` מרחיב ל-קו-המתאר של הבועה (0.55·w / 0.66·h). זה תיקן דלת/גשר/הכול.
- ✅ **מוקאפ גשר.** `gap` הורחב ל-[2,9]; האבנים פרוסות רחב על **סיפון הקשתות** (חלק מהגשר);
  התוויות **מדורגות בגובה** (אמצע גבוה) — קשת מעל האמה, בלי חפיפה; החצים מצביעים על האבנים
  האמיתיות. תשובה שגויה → האבן נופלת לתהום (קיים ב-`FAILS.bridge`).
- ⬜ **נותר:** אישור — במיוחד כיוון-הגשר.

איטרציה 10 — כיוונונים אחרונים (**הושלמה**, 12/12 + צילומים):
- ✅ **שגוי — רכיבה על כל המסלול הארוך.** מחליף את החזרה-החלקה מ-9: הקרונית רוכבת את **כל
  המסלול המפותל עד הסוף**, ה-tail מביא אותה לנוח **בדיוק מעל נקודת-ההתחלה**, ואז נפילה
  טבעית ישרה למטה אל הרצפה. (`FAILS.rail`, 5.8 שניות + fall.)
- ✅ **גשר — אבן של הגשר עצמו.** התשובה מצוירת כ**אבן-סיפון רחבה ושטוחה** (1.44×0.72 SPRITE)
  צמודה על ראש הקשתות — נקראת כאבן מהמעקה/סיפון של הגשר, לא לוח קטן מרחף מעליו.
- ⬜ **נותר:** אישור-כיוון לגשר (האם "אבן-סיפון רחבה" זה מה שהתכוונת ב"אבנים של הגשר עצמו").

---

## וריאציות (100 פריסות) — מוקפא
- הופעל תת-סוכן Opus שיקרא את האריחים ויעצב 100 פריסות-רקע.
- **הסוכן נכשל** — נגמרה מגבלת-השימוש של הסשן (מתאפס ~01:50 שעון ירושלים). הפלט `scratchpad/layouts.json` **חלקי/לא-ודאי**.
- **לא שולב כלום** ולא ישולב בלי אישור. תוכנית: להריץ שוב אחרי איפוס → להכין **דף-מגע ויזואלי** (thumbnails לפי משפחה) → המשתמש בוחר מה נכנס.

---

## תשתית (כלֵי-פיתוח, לא חלק מהמשחק)
> **נמחקו לפני קומיט** (קבצי-דיבאג): `_sp_harness.html`, `screens-preview.html`,
> `dungeon-first-room.html`, `_q_preview.html`, `tmp_test.txt`. ניתנים לשחזור בקלות בעת הצורך.
> הארטיפקטים המתארחים עדיין חיים (הקישורים למטה).
- **harness אימות** (נמחק) — היה מריץ את הרכיב האמיתי, מדלג לפריים דרך hash
  `#theme|kind|shuffle|mode|frac|WxH|hot` (modes: room/solved/flip/fail/failapp/over; `hot` = מסגרות אזורי-מגע).
  הוגש דרך `npx http-server`. **לשחזור** צריך לבנות מחדש (ראה היסטוריית השיחה).
- **viewer עצמאי** (נמחק) `screens-preview.html` — בורר-מסכים + ערכת-צבע + וריאציות + הדגמה חיה.
- **מסך scribble_dungeon ריק** (נמחק) `dungeon-first-room.html`.
- **ארטיפקטים חיים:** פלטפורמר https://claude.ai/code/artifact/2d895524-a786-4874-a47b-9ab6f8802746 ·
  dungeon https://claude.ai/code/artifact/4fd3dd1e-7d82-4c57-92e2-d236b97d252b ·
  אימות אחרון https://claude.ai/code/artifact/d48256cf-33a8-4bba-893f-9d433fc15189

---

## שינויים בקוד (עיקרי)
- `games/scribble-platformer.js`: אריחי-מבנה חדשים; `drawEnvironment` לכל חדר + וריאציות; `drawGate` (שער 2-קוביות); `drawLadder` (נפילה עם הדמות); `drawRails`/`drawRailPath`/`railTracks`/`sampleCurve` (רכבת-הרים); `EXITS`/`FAILS` per-kind; `beginFail`/`snapFail`/`finishFail`; פוזות `fall`/`recoil` ב-`drawPawn`; מנגנון `variant`/`groundForVariant`; מעקב-מצלמה ב-`step`.
- `service-worker.js`: הוספת האריחים ל-`PLATFORMER_ART` + בָּמְפּ cache **v54→v55→v56** (v56 = כל שינויי הסשן).
- **סדר החדרים רנדומלי:** `kindForDepth` בוחר סוג לפי hash של `depth`+`RUN_SALT` — סדר אקראי לכל
  ריצה, דטרמיניסטי בתוך ריצה, בחירה עצמאית (ייתכן אותו סוג פעמיים ברצף, ~11%). `KIND_CYCLE`
  הפך מרצף-קבוע ל**בריכת-סוגים**.
- אריחים חדשים (כבר על הדיסק): `tile_castle,tile_roof,tile_column,tile_arch,tile_archColumns,tile_archColumn,tile_archHalf,tile_slope,tile_blockDoor,tile_blockWindow,tile_hang,tile_fenceHigh,item_blaster`.

## מוסכמות
- הערות-קוד באנגלית; מחרוזות-UI בעברית.
- `apps.js` — הוספות רק בסוף (position-based ids).
- אלמנט-התשובה לא מסתיר את הציור (תוויות מורמות + חץ-מוביל).

## הצעדים הבאים
1. 🌉 **גשר** — אישור-כיוון ל"אבן-סיפון" (או מוקאפ אלטרנטיבי: אבן בתוך הקשתות).
2. ➕ מסך-מגדל-יורים — מוקאפ ואישור.
3. הרצת הסוכן מחדש (אחרי איפוס) → דף-מגע → אישור → שילוב 100 וריאציות.
4. ✅ ניקוי קבצי-דיבאג/תשתית — **בוצע** (`_sp_harness.html`, `screens-preview.html`,
   `dungeon-first-room.html`, `_q_preview.html`, `tmp_test.txt`). נותר: עדכון זיכרון.
